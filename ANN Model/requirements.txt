# ─────────────────────────────────────────────────────────────────────────────
# requirements.txt — Fins Heat Prediction (juinternshipenv)
# Derived from: main_code.ipynb
#   • Python version  : 3.10.19  (from notebook language_info metadata)
#   • TensorFlow      : 2.17.0   (from keras/src/layers path in cell-41 warning
#                                  → confirms Keras 3.x bundled with TF 2.17)
#   • NumPy           : <2.0.0   (hard constraint in TF 2.17 for Python ≤ 3.11)
#   • Conda env name  : juinternshipenv  (Windows, HP laptop)
# ─────────────────────────────────────────────────────────────────────────────

# Core numerical stack
numpy==1.26.4
pandas==2.2.2
scipy==1.13.1

# Deep learning
tensorflow==2.17.0
keras==3.4.1          # bundled by TF 2.17; keras>=3.2.0 required by TF 2.17

# Machine learning utilities
scikit-learn==1.5.1
joblib==1.4.2         # also a scikit-learn dependency; used directly for scaler persistence

# Visualisation
matplotlib==3.9.1

# ─────────────────────────────────────────────────────────────────────────────
# Install command (pip, inside your conda env):
#   pip install -r requirements.txt
#
# Or recreate the conda env from scratch:
#   conda create -n juinternshipenv python=3.10.19
#   conda activate juinternshipenv
#   pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────────────────
